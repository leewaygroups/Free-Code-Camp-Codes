'use strict';

var React = require('react');

var PomodoroDisplay = React.createClass({
  render: function () {
    return (
      <div>
        Display Pomodoro
      </div>
    );
  }
});

module.exports = PomodoroDisplay;