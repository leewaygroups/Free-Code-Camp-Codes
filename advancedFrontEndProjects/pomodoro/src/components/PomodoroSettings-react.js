'use strict';

var React = require('react');

var PomodoroSettings = React.createClass({
  render: function () {
    return (
      <div>
         Pomodoro Settings
      </div>
    );
  }
});

module.exports = PomodoroSettings;